# Node Starter App

A reusable NodeJS starter app that includes:
* NodeJS
* TypeScript
* Yarn
* Express
* Docker
* Makefile
* Hot loading

To run dev:
`make run`

To run production:
`make run-prod`

To stop:
`make down`
