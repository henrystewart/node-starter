# Node Starter App

Reusable NodeJS starter that includes:
[] NodeJS
* TypeScript
* Yarn
* Express
* Docker
* Makefile

To run dev:
`make run`

To run production:
`make run-prod`

To stop:
`make down`
